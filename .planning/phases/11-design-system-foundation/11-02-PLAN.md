---
phase: 11-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/shared/ConfidenceBadge.tsx
  - frontend/src/components/shared/ResultIndicator.tsx
  - frontend/src/components/shared/ErrorState.tsx
  - frontend/src/components/picks/PickCard.tsx
  - frontend/src/components/picks/SpreadLabel.tsx
  - frontend/src/components/experiments/ExperimentTable.tsx
  - frontend/src/components/experiments/ExperimentDetail.tsx
  - frontend/src/components/history/HistoryTable.tsx
  - frontend/src/components/history/HistoryLegend.tsx
  - frontend/src/components/accuracy/SummaryCards.tsx
  - frontend/src/components/accuracy/SpreadSummaryCards.tsx
  - frontend/src/pages/AccuracyPage.tsx
  - frontend/src/pages/ThisWeekPage.tsx
autonomous: false
requirements: [DSGN-03, DSGN-04]
must_haves:
  truths:
    - "No hardcoded Tailwind color classes (zinc-*, blue-*, gray-*, slate-*, green-*, red-*, amber-*) remain in any component TSX file or CSS file"
    - "ConfidenceBadge displays green/amber/red traffic-light colors per tier"
    - "PickCard left border displays green/amber/red per confidence tier"
    - "PickCard border and ConfidenceBadge use the SAME tier color for consistent signal"
    - "ResultIndicator uses status-success (green) for correct and status-error (red) for wrong"
    - "Sidebar active nav shows amber accent, inactive hover uses secondary surface"
    - "All shadcn/ui components render correctly after migration (no invisible text, broken borders)"
  artifacts:
    - path: "frontend/src/components/shared/ConfidenceBadge.tsx"
      provides: "Traffic-light tier styling via semantic tokens"
      contains: "bg-tier-high-bg text-tier-high"
    - path: "frontend/src/components/picks/PickCard.tsx"
      provides: "Traffic-light border styling via semantic tokens"
      contains: "border-tier-high"
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Sidebar with semantic token classes"
      contains: "bg-accent text-accent-foreground"
    - path: "frontend/src/components/shared/ResultIndicator.tsx"
      provides: "Status colors for correct/wrong indicators"
      contains: "text-status-success"
  key_links:
    - from: "frontend/src/components/shared/ConfidenceBadge.tsx"
      to: "frontend/src/index.css"
      via: "Tailwind utility classes generated from @theme inline tier tokens"
      pattern: "tier-high|tier-medium|tier-low"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "frontend/src/index.css"
      via: "Tailwind utility classes generated from @theme inline semantic tokens"
      pattern: "bg-accent|bg-secondary|border-border"
---

<objective>
Replace all ~36 hardcoded Tailwind color classes across 14 component files with semantic theme tokens defined in Plan 01's index.css.

Purpose: Eliminates all direct color references so the entire dashboard renders through the silverreyes.net design token system. After this plan, `grep -rEn "(zinc|blue|gray|slate)-[0-9]" frontend/src/` returns zero results.
Output: All 14 component files migrated to semantic tokens. Dashboard visually consistent with silverreyes.net identity.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-design-system-foundation/11-CONTEXT.md
@.planning/phases/11-design-system-foundation/11-UI-SPEC.md
@.planning/phases/11-design-system-foundation/11-01-SUMMARY.md

<interfaces>
<!-- Semantic tokens available from Plan 01 (registered in @theme inline): -->
<!--
  Existing shadcn tokens (now silverreyes.net values):
    bg-background, bg-card, bg-popover, bg-primary, bg-secondary, bg-muted, bg-accent, bg-destructive
    text-foreground, text-card-foreground, text-popover-foreground, text-primary, text-primary-foreground
    text-secondary-foreground, text-muted-foreground, text-accent-foreground, text-destructive
    border-border, border-input

  New custom tokens (defined in Plan 01):
    text-tier-high, bg-tier-high-bg       (green - high confidence)
    text-tier-medium, bg-tier-medium-bg   (amber - medium confidence)
    text-tier-low, bg-tier-low-bg         (desaturated red - low confidence)
    text-status-success, bg-status-success (green - correct/positive)
    text-status-error, bg-status-error     (desaturated red - wrong/negative)
    text-status-warning, bg-status-warning (amber - caution)

  Font utility:
    font-display (Syne, for headings)
    font-sans (IBM Plex Mono, default body)
    font-mono (IBM Plex Mono, same as sans)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded Tailwind color classes with semantic tokens</name>
  <files>
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/components/shared/ConfidenceBadge.tsx,
    frontend/src/components/shared/ResultIndicator.tsx,
    frontend/src/components/shared/ErrorState.tsx,
    frontend/src/components/picks/PickCard.tsx,
    frontend/src/components/picks/SpreadLabel.tsx,
    frontend/src/components/experiments/ExperimentTable.tsx,
    frontend/src/components/experiments/ExperimentDetail.tsx,
    frontend/src/components/history/HistoryTable.tsx,
    frontend/src/components/history/HistoryLegend.tsx,
    frontend/src/components/accuracy/SummaryCards.tsx,
    frontend/src/components/accuracy/SpreadSummaryCards.tsx,
    frontend/src/pages/AccuracyPage.tsx,
    frontend/src/pages/ThisWeekPage.tsx
  </files>
  <read_first>
    - frontend/src/components/layout/Sidebar.tsx
    - frontend/src/components/shared/ConfidenceBadge.tsx
    - frontend/src/components/shared/ResultIndicator.tsx
    - frontend/src/components/shared/ErrorState.tsx
    - frontend/src/components/picks/PickCard.tsx
    - frontend/src/components/picks/SpreadLabel.tsx
    - frontend/src/components/experiments/ExperimentTable.tsx
    - frontend/src/components/experiments/ExperimentDetail.tsx
    - frontend/src/components/history/HistoryTable.tsx
    - frontend/src/components/history/HistoryLegend.tsx
    - frontend/src/components/accuracy/SummaryCards.tsx
    - frontend/src/components/accuracy/SpreadSummaryCards.tsx
    - frontend/src/pages/AccuracyPage.tsx
    - frontend/src/pages/ThisWeekPage.tsx
    - frontend/src/index.css (verify tier/status tokens exist from Plan 01)
    - .planning/phases/11-design-system-foundation/11-UI-SPEC.md (authoritative color mapping)
  </read_first>
  <action>
Replace every hardcoded Tailwind color class with semantic token equivalents. Below is the COMPLETE file-by-file replacement map. For each file, find and replace the exact class strings listed.

**IMPORTANT:** Do NOT change any non-color classes. Do NOT restructure component logic. Only swap the color class strings.

---

### Sidebar.tsx (10 replacements)

Line 23 - Separator:
- BEFORE: `className="bg-zinc-800"`
- AFTER: `className="bg-border"`

Line 25 - Model status card:
- BEFORE: `className="border-zinc-800 bg-zinc-900/50"`
- AFTER: `className="border-border bg-background/50"`

Line 53 - Desktop sidebar aside:
- BEFORE: `border-zinc-800 bg-zinc-900` (inside the className string)
- AFTER: `border-border bg-card`

Line 70 - Active nav (desktop):
- BEFORE: `"bg-blue-500/10 text-blue-400"`
- AFTER: `"bg-accent text-accent-foreground"`

Line 71 - Inactive nav hover (desktop):
- BEFORE: `"text-muted-foreground hover:bg-zinc-800"`
- AFTER: `"text-muted-foreground hover:bg-secondary"`

Line 85 - Mobile nav:
- BEFORE: `border-zinc-800 bg-zinc-900` (inside the className string)
- AFTER: `border-border bg-card`

Line 98 - Active nav (mobile):
- BEFORE: `"bg-blue-500/10 text-blue-400"`
- AFTER: `"bg-accent text-accent-foreground"`

Line 99 - Inactive nav hover (mobile):
- BEFORE: `"text-muted-foreground hover:bg-zinc-800"`
- AFTER: `"text-muted-foreground hover:bg-secondary"`

---

### ConfidenceBadge.tsx (3 replacements)

Replace the entire `tierStyles` object (lines 4-8):
- BEFORE:
```typescript
const tierStyles: Record<string, string> = {
  high: "bg-blue-500/20 text-blue-400",
  medium: "bg-amber-500/20 text-amber-400",
  low: "bg-zinc-500/20 text-zinc-400",
};
```
- AFTER:
```typescript
const tierStyles: Record<string, string> = {
  high: "bg-tier-high-bg text-tier-high",
  medium: "bg-tier-medium-bg text-tier-medium",
  low: "bg-tier-low-bg text-tier-low",
};
```

---

### PickCard.tsx (3 replacements)

Replace the entire `tierBorderColors` object (lines 7-11):
- BEFORE:
```typescript
const tierBorderColors: Record<string, string> = {
  high: "border-blue-500",
  medium: "border-amber-500",
  low: "border-zinc-500",
};
```
- AFTER:
```typescript
const tierBorderColors: Record<string, string> = {
  high: "border-tier-high",
  medium: "border-tier-medium",
  low: "border-tier-low",
};
```

---

### ResultIndicator.tsx (3 replacements)

Line 9 - Correct icon:
- BEFORE: `className="h-5 w-5 text-green-500"`
- AFTER: `className="h-5 w-5 text-status-success"`

Line 13 - Wrong icon:
- BEFORE: `className="h-5 w-5 text-red-500"`
- AFTER: `className="h-5 w-5 text-status-error"`

Line 18 - Pending icon:
- BEFORE: `className="h-5 w-5 text-zinc-500"`
- AFTER: `className="h-5 w-5 text-muted-foreground"`

---

### ErrorState.tsx (1 replacement)

Line 21 - Alert icon:
- BEFORE: `className="h-8 w-8 text-red-500"`
- AFTER: `className="h-8 w-8 text-destructive"`

---

### SpreadLabel.tsx (3 replacements)

Replace the `getSpreadErrorColor` function body (lines 11-15):
- BEFORE:
```typescript
function getSpreadErrorColor(error: number): string {
  if (error <= 3) return "text-green-400";
  if (error <= 7) return "text-amber-400";
  return "text-red-400";
}
```
- AFTER:
```typescript
function getSpreadErrorColor(error: number): string {
  if (error <= 3) return "text-status-success";
  if (error <= 7) return "text-status-warning";
  return "text-status-error";
}
```

---

### ExperimentDetail.tsx (2 replacements)

Line 58 - SHAP bar background:
- BEFORE: `className="flex-1 h-4 bg-zinc-800 rounded overflow-hidden"`
- AFTER: `className="flex-1 h-4 bg-secondary rounded overflow-hidden"`

Line 60 - SHAP bar fill:
- BEFORE: `className="h-4 bg-blue-500 rounded"`
- AFTER: `className="h-4 bg-primary rounded"`

---

### ExperimentTable.tsx (4 replacements)

Line 91 - Row hover:
- BEFORE: `className="hover:bg-zinc-800/50 cursor-pointer transition-colors"`
- AFTER: `className="hover:bg-secondary/50 cursor-pointer transition-colors"`

Lines 116 - Kept badge:
- BEFORE: `className="bg-green-500/20 text-green-400 border-0"`
- AFTER: `className="bg-status-success/15 text-status-success border-0"`

Line 120 - Reverted badge:
- BEFORE: `className="bg-red-500/20 text-red-400 border-0"`
- AFTER: `className="bg-status-error/15 text-status-error border-0"`

Line 127 - Expanded row background:
- BEFORE: `className="bg-zinc-900/50"`
- AFTER: `className="bg-background/50"`

---

### HistoryTable.tsx (4 replacements)

Line 79 - Row hover:
- BEFORE: `className="hover:bg-zinc-800/50"`
- AFTER: `className="hover:bg-secondary/50"`

Replace the `getSpreadErrorColor` function body (lines 23-27):
- BEFORE:
```typescript
function getSpreadErrorColor(error: number): string {
  if (error <= 3) return "text-green-400";
  if (error <= 7) return "text-amber-400";
  return "text-red-400";
}
```
- AFTER:
```typescript
function getSpreadErrorColor(error: number): string {
  if (error <= 3) return "text-status-success";
  if (error <= 7) return "text-status-warning";
  return "text-status-error";
}
```

---

### HistoryLegend.tsx (~8 replacements)

Line 17 - Legend container:
- BEFORE: `className="mt-3 p-4 rounded-lg border border-zinc-800 bg-zinc-900/50 text-sm text-muted-foreground space-y-3"`
- AFTER: `className="mt-3 p-4 rounded-lg border border-border bg-background/50 text-sm text-muted-foreground space-y-3"`

Line 25 - Amber confidence text:
- BEFORE: `className="text-amber-400"`
- AFTER: `className="text-status-warning"`

Line 26 - Green confidence text:
- BEFORE: `className="text-green-400"`
- AFTER: `className="text-status-success"`

Line 43 - Green spread legend:
- BEFORE: `className="text-green-400 font-semibold"`
- AFTER: `className="text-status-success font-semibold"`

Line 47 - Amber spread legend:
- BEFORE: `className="text-amber-400 font-semibold"`
- AFTER: `className="text-status-warning font-semibold"`

Line 51 - Red spread legend:
- BEFORE: `className="text-red-400 font-semibold"`
- AFTER: `className="text-status-error font-semibold"`

Line 58 - Green result checkmark:
- BEFORE: `className="text-green-400"`
- AFTER: `className="text-status-success"`

Line 59 - Red result X:
- BEFORE: `className="text-red-400"`
- AFTER: `className="text-status-error"`

---

### AccuracyPage.tsx (1 replacement)

Line 227 - Week breakdown row hover:
- BEFORE: `className="hover:bg-zinc-800/50"`
- AFTER: `className="hover:bg-secondary/50"`

---

### ThisWeekPage.tsx (1 replacement)

Line 69 - "View Prediction History" link:
- BEFORE: `className="text-sm text-blue-400 hover:underline"`
- AFTER: `className="text-sm text-accent-foreground hover:underline"`

---

### SummaryCards.tsx (2 replacements)

Line 23 - Beating baseline badge:
- BEFORE: `className="bg-green-500/20 text-green-400 border-0"`
- AFTER: `className="bg-status-success/15 text-status-success border-0"`

Line 30 - Behind baseline badge:
- BEFORE: `className="bg-red-500/20 text-red-400 border-0"`
- AFTER: `className="bg-status-error/15 text-status-error border-0"`

---

### SpreadSummaryCards.tsx (2 replacements)

Line 57 - Beating badge:
- BEFORE: `className="bg-green-500/20 text-green-400 border-0"`
- AFTER: `className="bg-status-success/15 text-status-success border-0"`

Line 64 - Behind badge:
- BEFORE: `className="bg-red-500/20 text-red-400 border-0"`
- AFTER: `className="bg-status-error/15 text-status-error border-0"`

---

After ALL replacements, run the validation grep to verify zero remaining hardcoded classes:
```bash
grep -rEn "(zinc|blue|gray|slate|green|red|amber)-[0-9]" frontend/src/ --include="*.tsx" --include="*.css"
```

This should return ZERO results. If any remain, fix them using the semantic token mapping above.

NOTE: The :root block in index.css has chart-1 through chart-5 references and light-mode tokens, but those are CSS custom property DEFINITIONS (not Tailwind utility classes), so they do not match the `(zinc|blue|gray|slate|green|red|amber)-[0-9]` pattern. The validation grep is clean.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -rEn "(zinc|blue|gray|slate)-[0-9]" src/ --include="*.tsx" --include="*.css" | grep -v ":root" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEn "(zinc|blue|gray|slate)-[0-9]" frontend/src/ --include="*.tsx"` returns zero results
    - `grep -rEn "(blue)-[0-9]" frontend/src/ --include="*.tsx"` returns zero results
    - frontend/src/components/shared/ConfidenceBadge.tsx contains `bg-tier-high-bg text-tier-high`
    - frontend/src/components/shared/ConfidenceBadge.tsx contains `bg-tier-medium-bg text-tier-medium`
    - frontend/src/components/shared/ConfidenceBadge.tsx contains `bg-tier-low-bg text-tier-low`
    - frontend/src/components/picks/PickCard.tsx contains `border-tier-high`
    - frontend/src/components/picks/PickCard.tsx contains `border-tier-medium`
    - frontend/src/components/picks/PickCard.tsx contains `border-tier-low`
    - frontend/src/components/shared/ResultIndicator.tsx contains `text-status-success`
    - frontend/src/components/shared/ResultIndicator.tsx contains `text-status-error`
    - frontend/src/components/shared/ResultIndicator.tsx contains `text-muted-foreground`
    - frontend/src/components/shared/ErrorState.tsx contains `text-destructive`
    - frontend/src/components/layout/Sidebar.tsx contains `bg-accent text-accent-foreground`
    - frontend/src/components/layout/Sidebar.tsx contains `hover:bg-secondary`
    - frontend/src/components/layout/Sidebar.tsx contains `border-border bg-card`
    - frontend/src/components/layout/Sidebar.tsx contains `bg-border`
    - frontend/src/components/picks/SpreadLabel.tsx contains `text-status-success`
    - frontend/src/components/picks/SpreadLabel.tsx contains `text-status-warning`
    - frontend/src/components/picks/SpreadLabel.tsx contains `text-status-error`
    - frontend/src/components/experiments/ExperimentDetail.tsx contains `bg-secondary`
    - frontend/src/components/experiments/ExperimentDetail.tsx contains `bg-primary`
    - frontend/src/components/experiments/ExperimentTable.tsx contains `hover:bg-secondary/50`
    - frontend/src/components/experiments/ExperimentTable.tsx contains `bg-status-success/15 text-status-success`
    - frontend/src/components/experiments/ExperimentTable.tsx contains `bg-status-error/15 text-status-error`
    - frontend/src/components/experiments/ExperimentTable.tsx contains `bg-background/50`
    - frontend/src/components/history/HistoryTable.tsx contains `hover:bg-secondary/50`
    - frontend/src/components/history/HistoryTable.tsx contains `text-status-success` (in getSpreadErrorColor)
    - frontend/src/components/history/HistoryLegend.tsx contains `border-border bg-background/50`
    - frontend/src/components/history/HistoryLegend.tsx contains `text-status-success`
    - frontend/src/components/history/HistoryLegend.tsx contains `text-status-warning`
    - frontend/src/components/history/HistoryLegend.tsx contains `text-status-error`
    - frontend/src/components/accuracy/SummaryCards.tsx contains `bg-status-success/15 text-status-success`
    - frontend/src/components/accuracy/SummaryCards.tsx contains `bg-status-error/15 text-status-error`
    - frontend/src/components/accuracy/SpreadSummaryCards.tsx contains `bg-status-success/15 text-status-success`
    - frontend/src/components/accuracy/SpreadSummaryCards.tsx contains `bg-status-error/15 text-status-error`
    - frontend/src/pages/AccuracyPage.tsx contains `hover:bg-secondary/50`
    - frontend/src/pages/ThisWeekPage.tsx contains `text-accent-foreground`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    All ~36 hardcoded Tailwind color class instances across 14 files are replaced with semantic theme tokens. grep validation returns zero matches for zinc/blue/gray/slate-[0-9] in TSX files. Build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final visual verification of complete design system migration</name>
  <files>frontend/src/components/layout/Sidebar.tsx, frontend/src/components/shared/ConfidenceBadge.tsx, frontend/src/components/picks/PickCard.tsx</files>
  <read_first>
    - frontend/src/components/shared/ConfidenceBadge.tsx (verify tier tokens applied)
    - frontend/src/components/picks/PickCard.tsx (verify tier border tokens applied)
    - frontend/src/components/layout/Sidebar.tsx (verify accent tokens applied)
  </read_first>
  <action>
Human visual verification of the complete design system migration. No code changes -- this is a gate to confirm all 14 component files render correctly with the new semantic tokens.

What was built: Complete design system migration -- all 14 component files now use semantic tokens from the silverreyes.net palette. Traffic-light confidence tiers (green/amber/red), amber accent navigation, warm status colors throughout.

Steps to verify:
1. Run `cd frontend && npm run dev` to start the dev server
2. Open the dashboard in a browser (http://localhost:5173)

**This Week page:**
3. Verify PickCards show GREEN left border for high confidence, AMBER for medium, DESATURATED RED for low
4. Verify ConfidenceBadge colors MATCH their card's left border color (consistent signal)
5. Verify ResultIndicator shows green checkmark for correct, red X for wrong, muted dash for pending
6. If offseason link visible, verify "View Prediction History" link is amber (not blue)

**Accuracy page (/accuracy):**
7. Verify "Beating" badges show green text on green-tinted background
8. Verify "Behind" badges show desaturated red text on red-tinted background
9. Verify week-by-week table rows highlight on hover with a subtle warm surface color (not cool gray)

**Experiments page (/experiments):**
10. Verify "Kept" badge shows green, "Reverted" shows desaturated red
11. Verify SHAP progress bars use amber fill (bg-primary) on secondary background (not blue on zinc)
12. Click an experiment row to expand -- verify expanded area has dark background (bg-background/50)

**History page (/history):**
13. Verify table rows highlight on hover with warm surface color
14. Verify spread error colors: green for small error, amber for medium, red for large
15. Click "How to read this table" -- verify legend box has warm border and background (not cool zinc)
16. Verify legend text colors match: green, amber, red for spread accuracy descriptions

**Sidebar (all pages):**
17. Verify active nav item has amber text with subtle amber-tinted background
18. Verify inactive nav items show muted text with warm hover highlight
19. Verify sidebar border and background use warm tones (not cool zinc)
20. Check mobile nav (resize to <768px) -- same active/inactive styling applies

**Overall:**
21. Verify NO blue (#3b82f6-ish) colors appear ANYWHERE in the dashboard
22. Verify the overall feel is warm (amber/brown tones) not cool (blue/gray tones)

Resume signal: Type "approved" if the design system migration looks correct, or describe any issues.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - User confirms traffic-light confidence tier colors render correctly (green/amber/red)
    - User confirms PickCard border and ConfidenceBadge colors match per tier
    - User confirms sidebar active nav shows amber accent
    - User confirms no blue (#3b82f6-ish) colors appear anywhere in the dashboard
    - User confirms overall warm amber/brown palette across all pages
  </acceptance_criteria>
  <done>User types "approved" confirming all pages render with the correct silverreyes.net design system, traffic-light tiers, and no remaining blue/cool colors.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `grep -rEn "(zinc|blue|gray|slate)-[0-9]" frontend/src/ --include="*.tsx"` returns zero results
- `grep -rEn "green-[0-9]" frontend/src/ --include="*.tsx"` returns zero results
- `grep -rEn "red-[0-9]" frontend/src/ --include="*.tsx"` returns zero results
- `grep -rEn "amber-[0-9]" frontend/src/ --include="*.tsx"` returns zero results
- `grep -c "tier-high" frontend/src/components/shared/ConfidenceBadge.tsx` returns at least 1
- `grep -c "border-tier" frontend/src/components/picks/PickCard.tsx` returns at least 1
- `grep -c "text-status-success" frontend/src/components/shared/ResultIndicator.tsx` returns at least 1
</verification>

<success_criteria>
- Zero hardcoded Tailwind color classes remain in any TSX file
- All confidence tiers display in traffic-light colors (green/amber/red) per user decision
- Card border and badge show the SAME tier color for consistent signal
- All status indicators use semantic tokens (status-success, status-error, status-warning)
- Sidebar uses accent tokens for active state and secondary for hover
- Build succeeds with zero errors
- Human verifies complete visual consistency with silverreyes.net warm amber palette
</success_criteria>

<output>
After completion, create `.planning/phases/11-design-system-foundation/11-02-SUMMARY.md`
</output>

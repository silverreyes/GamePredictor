---
phase: 11-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/index.css
  - frontend/src/main.tsx
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-04]
must_haves:
  truths:
    - "Dashboard background is near-black (#080807) matching silverreyes.net"
    - "Amber accent (#f0a020) appears on active nav items and interactive elements"
    - "All headings render in Syne font family"
    - "All body text renders in IBM Plex Mono font family"
    - "No flash of unstyled text on page load (fonts self-hosted via @fontsource)"
    - "shadcn/ui buttons, cards, tooltips, dropdowns display correctly with remapped tokens"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Complete silverreyes.net palette in .dark block, tier/status tokens, @fontsource font imports, @theme inline with font-display and tier/status color registrations"
      contains: "oklch(0.134 0.003 106.7)"
    - path: "frontend/src/main.tsx"
      provides: "@fontsource CSS imports for Syne and IBM Plex Mono"
      contains: "@fontsource/syne"
  key_links:
    - from: "frontend/src/index.css"
      to: "shadcn/ui components"
      via: "CSS custom properties consumed by Tailwind utilities"
      pattern: "--background.*oklch\\(0\\.134"
    - from: "frontend/src/index.css"
      to: "frontend/src/main.tsx"
      via: "@fontsource imports loaded before index.css"
      pattern: "@fontsource/syne"
---

<objective>
Migrate the dashboard theme foundation to the silverreyes.net visual identity: warm amber palette, Syne + IBM Plex Mono typography, and semantic tier/status tokens.

Purpose: Establishes the color and font foundation that all components consume. Plan 02 depends on the custom tokens (tier-high, status-success, etc.) registered here.
Output: Updated index.css with silverreyes.net palette, @fontsource font packages installed, @theme inline extended with new tokens.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-design-system-foundation/11-CONTEXT.md
@.planning/phases/11-design-system-foundation/11-RESEARCH.md
@.planning/phases/11-design-system-foundation/11-UI-SPEC.md

<interfaces>
<!-- Current index.css structure (executor must preserve architecture, only change values) -->
<!-- From frontend/src/index.css: -->
<!-- Line 1-5: Imports (@import "tailwindcss", Google Fonts CDN, tw-animate-css, shadcn/tailwind.css, @custom-variant dark) -->
<!-- Line 7-12: body rule with font-family -->
<!-- Line 14-47: :root block (light mode values - unused in v1.2) -->
<!-- Line 49-81: .dark block (active values - these get rewritten) -->
<!-- Line 83-124: @theme inline block (Tailwind utility generation - gets extended) -->
<!-- Line 126-136: @layer base block (border, body, html base styles) -->

<!-- From frontend/src/main.tsx: -->
<!-- Line 1-4: imports (StrictMode, createRoot, index.css, App) -->
<!-- Line 6-10: createRoot render -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fonts and rewrite theme foundation</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/index.css, frontend/src/main.tsx</files>
  <read_first>
    - frontend/src/index.css (current theme tokens, @theme inline block, imports)
    - frontend/src/main.tsx (current import structure)
    - frontend/package.json (current dependencies)
    - .planning/phases/11-design-system-foundation/11-UI-SPEC.md (authoritative design contract)
  </read_first>
  <action>
**Step 1: Install @fontsource packages**

Run from `frontend/` directory:
```bash
npm install @fontsource/syne @fontsource/ibm-plex-mono
```

Per UI-SPEC Font Loading table, only these weights are needed:
- Syne: 400, 700
- IBM Plex Mono: 400, 600

**Step 2: Add @fontsource CSS imports to main.tsx**

Add these imports BEFORE the `./index.css` import in `frontend/src/main.tsx`:
```typescript
import '@fontsource/syne/400.css'
import '@fontsource/syne/700.css'
import '@fontsource/ibm-plex-mono/400.css'
import '@fontsource/ibm-plex-mono/600.css'
```

The file should read:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import '@fontsource/syne/400.css'
import '@fontsource/syne/700.css'
import '@fontsource/ibm-plex-mono/400.css'
import '@fontsource/ibm-plex-mono/600.css'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

**Step 3: Rewrite frontend/src/index.css**

Replace the ENTIRE contents of index.css with the following. This preserves the existing architecture (imports, :root, .dark, @theme inline, @layer base) but changes VALUES to match silverreyes.net palette and adds new tokens.

Key changes from current state:
- Line 2: DELETE the Google Fonts CDN import (`@import url('https://fonts.googleapis.com/...')`)
- Body rule: Change font-family from Inter to `'IBM Plex Mono', ui-monospace, monospace`
- :root block: Add comment noting these are unused in v1.2 (dark-only). Leave existing light mode values as-is for v1.3 future-proofing.
- .dark block: Replace ALL oklch values per the Complete shadcn Token Mapping table in UI-SPEC. Add tier tokens and status tokens.
- @theme inline: Replace --font-sans and --font-mono. Add --font-display. Add all --color-tier-* and --color-status-* registrations.
- @layer base: Add `h1, h2, h3 { font-family: var(--font-display); }` rule for automatic heading styling.

The complete new index.css content:

```css
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";
@custom-variant dark (&:is(.dark *));

body {
  margin: 0;
  font-family: 'IBM Plex Mono', ui-monospace, monospace;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Light mode values -- unused in v1.2 (dark-only). Preserved for v1.3 light mode. */
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  --primary: oklch(0.205 0 0);
  --primary-foreground: oklch(0.985 0 0);
  --secondary: oklch(0.97 0 0);
  --secondary-foreground: oklch(0.205 0 0);
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.556 0 0);
  --accent: oklch(0.97 0 0);
  --accent-foreground: oklch(0.205 0 0);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.922 0 0);
  --input: oklch(0.922 0 0);
  --ring: oklch(0.708 0 0);
  --chart-1: oklch(0.809 0.105 251.813);
  --chart-2: oklch(0.623 0.214 259.815);
  --chart-3: oklch(0.546 0.245 262.881);
  --chart-4: oklch(0.488 0.243 264.376);
  --chart-5: oklch(0.424 0.199 265.638);
  --radius: 0.625rem;
  --sidebar: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.145 0 0);
  --sidebar-primary: oklch(0.205 0 0);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.97 0 0);
  --sidebar-accent-foreground: oklch(0.205 0 0);
  --sidebar-border: oklch(0.922 0 0);
  --sidebar-ring: oklch(0.708 0 0);
}

/* silverreyes.net palette (extracted 2026-03-24) */
.dark {
  --background: oklch(0.134 0.003 106.7);
  --foreground: oklch(0.883 0.015 84.6);
  --card: oklch(0.177 0.002 106.6);
  --card-foreground: oklch(0.883 0.015 84.6);
  --popover: oklch(0.177 0.002 106.6);
  --popover-foreground: oklch(0.883 0.015 84.6);
  --primary: oklch(0.767 0.157 71.7);
  --primary-foreground: oklch(0.134 0.003 106.7);
  --secondary: oklch(0.214 0.004 84.6);
  --secondary-foreground: oklch(0.883 0.015 84.6);
  --muted: oklch(0.214 0.004 84.6);
  --muted-foreground: oklch(0.561 0.019 84.6);
  --accent: oklch(0.767 0.157 71.7 / 12%);
  --accent-foreground: oklch(0.767 0.157 71.7);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(0.260 0.007 95.3);
  --input: oklch(0.260 0.007 95.3);
  --ring: oklch(0.767 0.157 71.7);
  --chart-1: oklch(0.809 0.105 251.813);
  --chart-2: oklch(0.623 0.214 259.815);
  --chart-3: oklch(0.546 0.245 262.881);
  --chart-4: oklch(0.488 0.243 264.376);
  --chart-5: oklch(0.424 0.199 265.638);

  /* Sidebar tokens (mirror main theme) */
  --sidebar: oklch(0.177 0.002 106.6);
  --sidebar-foreground: oklch(0.883 0.015 84.6);
  --sidebar-primary: oklch(0.767 0.157 71.7);
  --sidebar-primary-foreground: oklch(0.883 0.015 84.6);
  --sidebar-accent: oklch(0.767 0.157 71.7 / 12%);
  --sidebar-accent-foreground: oklch(0.767 0.157 71.7);
  --sidebar-border: oklch(0.260 0.007 95.3);
  --sidebar-ring: oklch(0.767 0.157 71.7);

  /* Confidence tier tokens (traffic-light system) */
  --tier-high: oklch(0.72 0.17 142);
  --tier-high-bg: oklch(0.72 0.17 142 / 15%);
  --tier-medium: oklch(0.767 0.157 71.7);
  --tier-medium-bg: oklch(0.767 0.157 71.7 / 15%);
  --tier-low: oklch(0.65 0.15 25);
  --tier-low-bg: oklch(0.65 0.15 25 / 15%);

  /* Semantic status colors */
  --status-success: oklch(0.72 0.17 142);
  --status-error: oklch(0.65 0.15 25);
  --status-warning: oklch(0.767 0.157 71.7);
}

@theme inline {
  --font-sans: 'IBM Plex Mono', ui-monospace, monospace;
  --font-mono: 'IBM Plex Mono', ui-monospace, monospace;
  --font-display: 'Syne', sans-serif;
  --color-sidebar-ring: var(--sidebar-ring);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar: var(--sidebar);
  --color-chart-5: var(--chart-5);
  --color-chart-4: var(--chart-4);
  --color-chart-3: var(--chart-3);
  --color-chart-2: var(--chart-2);
  --color-chart-1: var(--chart-1);
  --color-ring: var(--ring);
  --color-input: var(--input);
  --color-border: var(--border);
  --color-destructive: var(--destructive);
  --color-accent-foreground: var(--accent-foreground);
  --color-accent: var(--accent);
  --color-muted-foreground: var(--muted-foreground);
  --color-muted: var(--muted);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-secondary: var(--secondary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary: var(--primary);
  --color-popover-foreground: var(--popover-foreground);
  --color-popover: var(--popover);
  --color-card-foreground: var(--card-foreground);
  --color-card: var(--card);
  --color-foreground: var(--foreground);
  --color-background: var(--background);
  --color-tier-high: var(--tier-high);
  --color-tier-high-bg: var(--tier-high-bg);
  --color-tier-medium: var(--tier-medium);
  --color-tier-medium-bg: var(--tier-medium-bg);
  --color-tier-low: var(--tier-low);
  --color-tier-low-bg: var(--tier-low-bg);
  --color-status-success: var(--status-success);
  --color-status-error: var(--status-error);
  --color-status-warning: var(--status-warning);
  --radius-sm: calc(var(--radius) * 0.6);
  --radius-md: calc(var(--radius) * 0.8);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) * 1.4);
  --radius-2xl: calc(var(--radius) * 1.8);
  --radius-3xl: calc(var(--radius) * 2.2);
  --radius-4xl: calc(var(--radius) * 2.6);
}

@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
  html {
    @apply font-sans;
  }
  h1, h2, h3 {
    font-family: var(--font-display);
  }
}
```

IMPORTANT notes:
- The Google Fonts CDN import line is REMOVED (not just commented out). This eliminates the double-load / FOUT risk (Pitfall 3 from RESEARCH).
- The :root block is LEFT UNCHANGED (future-proofing for v1.3 light mode).
- The .dark block has ALL values replaced per UI-SPEC Complete shadcn Token Mapping.
- --primary-foreground is set to the dark background color (oklch(0.134 0.003 106.7)) because --primary is now the bright amber accent. This ensures text on primary buttons has contrast (Pitfall 1 from RESEARCH).
- Tier and status tokens are defined in .dark AND registered in @theme inline with --color- prefix so Tailwind generates utility classes like `text-tier-high`, `bg-tier-high-bg`, `text-status-success` etc. (Pitfall 2 from RESEARCH).
- `h1, h2, h3` base layer rule applies Syne automatically to all headings per UI-SPEC Font Assignment Rules.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/index.css does NOT contain `fonts.googleapis.com`
    - frontend/src/index.css contains `oklch(0.134 0.003 106.7)` (silverreyes.net background)
    - frontend/src/index.css contains `oklch(0.767 0.157 71.7)` (amber accent as --primary)
    - frontend/src/index.css contains `--tier-high: oklch(0.72 0.17 142)`
    - frontend/src/index.css contains `--tier-low: oklch(0.65 0.15 25)`
    - frontend/src/index.css contains `--status-success: oklch(0.72 0.17 142)`
    - frontend/src/index.css contains `--color-tier-high: var(--tier-high)` in @theme inline
    - frontend/src/index.css contains `--color-status-success: var(--status-success)` in @theme inline
    - frontend/src/index.css contains `--font-display: 'Syne', sans-serif` in @theme inline
    - frontend/src/index.css contains `--font-sans: 'IBM Plex Mono'` in @theme inline
    - frontend/src/index.css contains `font-family: var(--font-display)` in base layer (h1, h2, h3 rule)
    - frontend/src/index.css body rule contains `font-family: 'IBM Plex Mono'`
    - frontend/src/main.tsx contains `import '@fontsource/syne/400.css'`
    - frontend/src/main.tsx contains `import '@fontsource/syne/700.css'`
    - frontend/src/main.tsx contains `import '@fontsource/ibm-plex-mono/400.css'`
    - frontend/src/main.tsx contains `import '@fontsource/ibm-plex-mono/600.css'`
    - frontend/package.json contains `@fontsource/syne`
    - frontend/package.json contains `@fontsource/ibm-plex-mono`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    index.css contains the complete silverreyes.net palette in .dark block, all tier/status custom tokens, @fontsource font declarations in @theme inline, and h1-h3 heading rule. main.tsx imports the 4 @fontsource weight CSS files. Google Fonts CDN import is removed. Build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify theme foundation renders correctly</name>
  <files>frontend/src/index.css, frontend/src/main.tsx</files>
  <read_first>
    - frontend/src/index.css (verify token values are correct)
    - frontend/src/main.tsx (verify @fontsource imports present)
  </read_first>
  <action>
Human visual verification of the theme foundation migration. No code changes -- this is a gate to confirm Task 1's output renders correctly before proceeding to Plan 02.

What was built: Complete theme foundation migration -- silverreyes.net warm amber palette, Syne headings, IBM Plex Mono body text, semantic tier/status tokens registered in Tailwind. All existing shadcn/ui components consume the new token values.

Steps to verify:
1. Run `cd frontend && npm run dev` to start the dev server
2. Open the dashboard in a browser (http://localhost:5173)
3. Verify BACKGROUND: Page background should be near-black (#080807), not the previous neutral dark gray
4. Verify TEXT: Body text should render in IBM Plex Mono (monospace font with distinctive lowercase 'l' and '1'). Compare against Inter (the previous sans-serif font)
5. Verify HEADINGS: Any h1/h2/h3 page titles should render in Syne (a geometric sans-serif, noticeably different from the monospace body text)
6. Verify CARDS: Card backgrounds should be a slightly lighter dark (#111110), with warm-toned borders (#252420) instead of the previous cool gray borders
7. Verify BUTTONS: Any shadcn Button components should still be functional -- text visible, not blending into background
8. Verify FONTS: Hard-refresh (Ctrl+Shift+R) and watch for any flash of unstyled text. Fonts should load immediately since they are self-hosted via @fontsource
9. Open DevTools Network tab, filter by "font" -- verify NO requests to fonts.googleapis.com (all fonts should be served from the app bundle)

NOTE: Hardcoded color classes (zinc-800, blue-500, etc.) in components like Sidebar, ConfidenceBadge, PickCard will still show OLD colors. That is expected -- Plan 02 replaces those. Only the semantic token-based colors should have changed.

Resume signal: Type "approved" to continue to Plan 02, or describe any issues with the theme foundation.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - User confirms near-black background renders correctly
    - User confirms IBM Plex Mono body text and Syne headings render correctly
    - User confirms no flash of unstyled text on hard refresh
    - User confirms no requests to fonts.googleapis.com in Network tab
    - User confirms shadcn/ui components (buttons, cards) display with visible text and borders
  </acceptance_criteria>
  <done>User types "approved" confirming the theme foundation renders correctly with silverreyes.net visual identity.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds (type-check + build)
- `grep -c "fonts.googleapis.com" frontend/src/index.css` returns 0
- `grep -c "oklch(0.134 0.003 106.7)" frontend/src/index.css` returns at least 1
- `grep -c "@fontsource/syne" frontend/src/main.tsx` returns at least 1
</verification>

<success_criteria>
- Build succeeds with zero errors
- Google Fonts CDN reference removed from index.css
- .dark block contains all silverreyes.net oklch values per UI-SPEC
- @theme inline registers tier, status, and font-display tokens
- @fontsource packages installed and imported in main.tsx
- Human verifies visual appearance matches silverreyes.net warm amber palette
</success_criteria>

<output>
After completion, create `.planning/phases/11-design-system-foundation/11-01-SUMMARY.md`
</output>

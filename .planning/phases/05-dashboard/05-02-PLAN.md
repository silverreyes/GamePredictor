---
phase: 05-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/ThisWeekPage.tsx
  - frontend/src/pages/AccuracyPage.tsx
  - frontend/src/pages/ExperimentsPage.tsx
  - frontend/src/pages/HistoryPage.tsx
  - frontend/src/components/picks/PickCard.tsx
  - frontend/src/components/picks/PicksGrid.tsx
  - frontend/src/components/accuracy/SummaryCards.tsx
  - frontend/src/components/experiments/ExperimentTable.tsx
  - frontend/src/components/experiments/ExperimentDetail.tsx
  - frontend/src/components/history/HistoryTable.tsx
  - frontend/src/components/history/HistoryFilters.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "User sees this week's games with predicted winner, win probability percentage, and confidence tier badge on the landing page"
    - "User sees season accuracy summary comparing model record against always-home and better-record baselines"
    - "User sees experiment scoreboard listing all experiments with 2023 val accuracy, sortable columns, and expandable detail rows"
    - "User sees historical predictions with correct/incorrect icons, and can filter by season and team via URL-synced dropdowns"
    - "All pages show loading skeletons while data fetches and error states with retry buttons on failure"
  artifacts:
    - path: "frontend/src/pages/ThisWeekPage.tsx"
      provides: "Landing page with pick cards grid"
      contains: "useCurrentPredictions"
    - path: "frontend/src/components/picks/PickCard.tsx"
      provides: "Individual game prediction card"
      contains: "ConfidenceBadge"
    - path: "frontend/src/pages/AccuracyPage.tsx"
      provides: "Season accuracy with baseline comparison"
      contains: "usePredictionHistory"
    - path: "frontend/src/components/accuracy/SummaryCards.tsx"
      provides: "3 summary cards: model, always-home, better-record"
      contains: "Beating"
    - path: "frontend/src/pages/ExperimentsPage.tsx"
      provides: "Experiment scoreboard with sortable table"
      contains: "useExperiments"
    - path: "frontend/src/components/experiments/ExperimentTable.tsx"
      provides: "Sortable experiment table with expandable rows"
      contains: "Collapsible"
    - path: "frontend/src/pages/HistoryPage.tsx"
      provides: "Prediction history with filters"
      contains: "useSearchParams"
    - path: "frontend/src/components/history/HistoryTable.tsx"
      provides: "History table with result indicators"
      contains: "ResultIndicator"
    - path: "frontend/src/components/history/HistoryFilters.tsx"
      provides: "Season and team filter dropdowns"
      contains: "Select"
  key_links:
    - from: "frontend/src/pages/ThisWeekPage.tsx"
      to: "/api/predictions/current"
      via: "useCurrentPredictions hook"
      pattern: "useCurrentPredictions"
    - from: "frontend/src/pages/AccuracyPage.tsx"
      to: "/api/predictions/history"
      via: "usePredictionHistory hook"
      pattern: "usePredictionHistory"
    - from: "frontend/src/pages/AccuracyPage.tsx"
      to: "/api/model/info"
      via: "useModelInfo hook"
      pattern: "useModelInfo"
    - from: "frontend/src/pages/ExperimentsPage.tsx"
      to: "/api/experiments"
      via: "useExperiments hook"
      pattern: "useExperiments"
    - from: "frontend/src/pages/HistoryPage.tsx"
      to: "/api/predictions/history"
      via: "usePredictionHistory with URL-synced filters"
      pattern: "useSearchParams"
---

<objective>
Build all 4 page views consuming the hooks and shared components from Plan 01: This Week's Picks (pick cards grid), Season Accuracy (summary cards with baseline comparison), Experiment Scoreboard (sortable table with expandable detail rows), and Prediction History (filterable table with correct/incorrect indicators). Then verify all pages visually with the API running.

Purpose: Complete the dashboard UI so all 4 DASH requirements are satisfied end-to-end.

Output: Fully functional 4-page dashboard rendering real API data with loading states, error states, and responsive layout.
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
@.planning/phases/05-dashboard/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types, hooks, and shared components available for import -->

From frontend/src/lib/types.ts:
```typescript
export interface PredictionResponse {
  game_id: string;
  season: number;
  week: number;
  game_date: string | null;
  home_team: string;
  away_team: string;
  predicted_winner: string;
  confidence: number;
  confidence_tier: "high" | "medium" | "low";
  actual_winner: string | null;
  correct: boolean | null;
}

export interface HistorySummary {
  correct: number;
  total: number;
  accuracy: number | null;
}

export interface PredictionHistoryResponse {
  predictions: PredictionResponse[];
  summary: HistorySummary;
}

export interface WeekPredictionsResponse {
  season: number;
  week: number;
  status: "ok" | "offseason";
  predictions: PredictionResponse[];
}

export interface ModelInfoResponse {
  experiment_id: number;
  training_date: string;
  val_accuracy_2023: number;
  feature_count: number;
  hypothesis: string;
  baseline_always_home: number;
  baseline_better_record: number;
}

export interface ExperimentResponse {
  experiment_id: number;
  timestamp: string;
  params: Record<string, unknown>;
  features: string[];
  val_accuracy_2023: number;
  val_accuracy_2022: number;
  val_accuracy_2021: number;
  baseline_always_home: number;
  baseline_better_record: number;
  log_loss: number;
  brier_score: number;
  shap_top5: ShapFeature[];
  keep: boolean;
  hypothesis: string;
  prev_best_acc: number;
  model_path: string | null;
}
```

Hooks available (all from `@/hooks/`):
- `useCurrentPredictions()` -> `{ data, isLoading, isError, error, refetch }`
- `usePredictionHistory(season?, team?)` -> same shape
- `useModelInfo()` -> same shape
- `useExperiments()` -> same shape

Shared components available (all from `@/components/shared/`):
- `<ConfidenceBadge tier="high"|"medium"|"low" />`
- `<ErrorState heading="..." body="..." onRetry={fn} />`
- `<ResultIndicator correct={true|false|null} />`

shadcn components available (all from `@/components/ui/`):
- Card, CardContent, CardHeader, CardTitle
- Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- Badge
- Skeleton
- Select, SelectContent, SelectItem, SelectTrigger, SelectValue
- Collapsible, CollapsibleContent, CollapsibleTrigger
- Separator
- Button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build This Week's Picks and Season Accuracy pages</name>
  <files>
    frontend/src/pages/ThisWeekPage.tsx,
    frontend/src/components/picks/PickCard.tsx,
    frontend/src/components/picks/PicksGrid.tsx,
    frontend/src/pages/AccuracyPage.tsx,
    frontend/src/components/accuracy/SummaryCards.tsx
  </files>
  <read_first>
    frontend/src/lib/types.ts,
    frontend/src/hooks/useCurrentPredictions.ts,
    frontend/src/hooks/usePredictionHistory.ts,
    frontend/src/hooks/useModelInfo.ts,
    frontend/src/components/shared/ConfidenceBadge.tsx,
    frontend/src/components/shared/ErrorState.tsx,
    frontend/src/components/shared/ResultIndicator.tsx,
    .planning/phases/05-dashboard/05-UI-SPEC.md
  </read_first>
  <action>
**A. PickCard component** — Create `frontend/src/components/picks/PickCard.tsx`:

Props: `prediction: PredictionResponse`.

Layout per UI-SPEC pick card wireframe:
- Card with `border-l-[3px]` colored by confidence tier: high=`border-blue-500`, medium=`border-amber-500`, low=`border-zinc-500`.
- Subtle hover: `hover:-translate-y-px transition-transform duration-150` (display-only, not clickable).
- Top line: `{away_team} @ {home_team}` at 14px regular (text-sm). The `@` separator in muted text (`text-muted-foreground`).
- Middle: predicted winner name at 20px semibold (`text-xl font-semibold`). Non-predicted team displayed at 14px muted text.
- Win probability: `{(confidence * 100).toFixed(1)}%` at 28px semibold (`text-[28px] font-semibold leading-tight`).
- Bottom: `<ConfidenceBadge tier={prediction.confidence_tier} />`.
- Top-right corner: if `actual_winner` is set, show `<ResultIndicator correct={prediction.correct} />` (green check or red X, 16px). Use `absolute top-3 right-3` positioning within `relative` card.

**B. PicksGrid component** — Create `frontend/src/components/picks/PicksGrid.tsx`:

Props: `predictions: PredictionResponse[]`.

CSS Grid container: `grid gap-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-3`. Minimum card width 280px via `min-w-[280px]`.

Map over predictions rendering `<PickCard key={prediction.game_id} prediction={prediction} />`.

**C. ThisWeekPage** — Rewrite `frontend/src/pages/ThisWeekPage.tsx`:

- Call `useCurrentPredictions()` hook.
- Set `document.title` to `"Week {data.week} Picks - NFL Predictor"` when data is available (use `useEffect`).
- Loading state: render page heading skeleton (Skeleton `h-7 w-48`) + 3 card-shaped skeletons in grid layout (`h-48 rounded-lg` each). Use the same grid layout as PicksGrid.
- Error state: render `<ErrorState heading="Connection Failed" body="Could not reach the prediction API. Make sure the server is running on localhost:8000." onRetry={() => refetch()} />`.
- Offseason state (when `data.status === "offseason"`): render `<ErrorState heading="Offseason" body="No upcoming games to predict. Check back when the regular season starts." />` with a Link to `/history` as additional content.
- Normal state: render `<h1 className="text-xl font-semibold mb-1">Week {data.week} Picks</h1>`, `<p className="text-sm text-muted-foreground mb-8">{data.season} Season</p>`, then `<PicksGrid predictions={data.predictions} />`.

**D. SummaryCards component** — Create `frontend/src/components/accuracy/SummaryCards.tsx`:

Props: `summary: HistorySummary`, `baselineAlwaysHome: number`, `baselineBetterRecord: number`.

Render 3 cards in a flex row (`flex flex-col md:flex-row gap-6`):

Card 1 — "MODEL":
- Title: "Model" at `text-sm text-muted-foreground uppercase tracking-wide`.
- Display number: `{summary.correct}/{summary.total}` at 28px semibold.
- Below: `{summary.accuracy !== null ? (summary.accuracy * 100).toFixed(1) + "%" : "N/A"}`.

Card 2 — "VS ALWAYS-HOME":
- Title: "vs Always-Home" at text-sm muted.
- Display number: `{(baselineAlwaysHome * 100).toFixed(1)}%` at 28px semibold.
- Comparison badge: compute `diff = (summary.accuracy ?? 0) - baselineAlwaysHome`. If `diff > 0`: green Badge with text `Beating +{(diff*100).toFixed(1)}%`. If `diff <= 0`: red Badge with text `Behind {(diff*100).toFixed(1)}%`.

Card 3 — "VS BETTER-RECORD":
- Same structure as Card 2 but comparing against `baselineBetterRecord`.

**E. AccuracyPage** — Rewrite `frontend/src/pages/AccuracyPage.tsx`:

- Call `usePredictionHistory()` hook (no filters — shows all for current season).
- Call `useModelInfo()` hook (for baseline values).
- Set `document.title` to `"Season Accuracy - NFL Predictor"`.
- Loading state: 3 card skeletons (`h-32 rounded-lg`) in flex row + table skeleton (5 rows of `h-8`).
- Error state: `<ErrorState heading="Connection Failed" body="Could not reach the prediction API. Make sure the server is running on localhost:8000." onRetry={() => { historyQuery.refetch(); modelQuery.refetch(); }} />`.
- Normal state: `<h1 className="text-xl font-semibold mb-8">Season Accuracy</h1>`, then `<SummaryCards summary={historyData.summary} baselineAlwaysHome={modelData.baseline_always_home} baselineBetterRecord={modelData.baseline_better_record} />`.
- Below summary cards: Week-by-week breakdown table (computed client-side from `historyData.predictions`). Group predictions by week, compute per-week `correct/total`. Render as shadcn Table with columns: Week (number), Record ("X/Y"), Accuracy ("XX.X%"). Use `Table`, `TableHeader`, `TableHead`, `TableBody`, `TableRow`, `TableCell` from shadcn. Row hover: `hover:bg-zinc-800/50`.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/picks/PickCard.tsx contains "ConfidenceBadge"
    - frontend/src/components/picks/PickCard.tsx contains "ResultIndicator"
    - frontend/src/components/picks/PickCard.tsx contains "confidence_tier"
    - frontend/src/components/picks/PickCard.tsx contains "border-blue-500"
    - frontend/src/components/picks/PickCard.tsx contains "border-amber-500"
    - frontend/src/components/picks/PickCard.tsx contains "text-[28px]" or "text-3xl" (Display size for probability)
    - frontend/src/components/picks/PicksGrid.tsx contains "grid-cols-1"
    - frontend/src/components/picks/PicksGrid.tsx contains "grid-cols-3" or "lg:grid-cols-3"
    - frontend/src/pages/ThisWeekPage.tsx contains "useCurrentPredictions"
    - frontend/src/pages/ThisWeekPage.tsx contains "Offseason"
    - frontend/src/pages/ThisWeekPage.tsx contains "PicksGrid"
    - frontend/src/pages/ThisWeekPage.tsx contains "ErrorState"
    - frontend/src/pages/ThisWeekPage.tsx contains "Skeleton"
    - frontend/src/components/accuracy/SummaryCards.tsx contains "Beating"
    - frontend/src/components/accuracy/SummaryCards.tsx contains "Behind"
    - frontend/src/components/accuracy/SummaryCards.tsx contains "baseline_always_home" or "baselineAlwaysHome"
    - frontend/src/components/accuracy/SummaryCards.tsx contains "baseline_better_record" or "baselineBetterRecord"
    - frontend/src/pages/AccuracyPage.tsx contains "usePredictionHistory"
    - frontend/src/pages/AccuracyPage.tsx contains "useModelInfo"
    - frontend/src/pages/AccuracyPage.tsx contains "SummaryCards"
    - frontend/src/pages/AccuracyPage.tsx contains "Season Accuracy"
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>This Week's Picks page renders pick cards with predicted winner, win probability, and confidence tier badge. Season Accuracy page renders 3 summary cards comparing model record against both baselines with Beating/Behind badges, plus a week-by-week breakdown table. Both pages handle loading skeletons, error states with retry, and offseason state.</done>
</task>

<task type="auto">
  <name>Task 2: Build Experiments Scoreboard and Prediction History pages</name>
  <files>
    frontend/src/pages/ExperimentsPage.tsx,
    frontend/src/components/experiments/ExperimentTable.tsx,
    frontend/src/components/experiments/ExperimentDetail.tsx,
    frontend/src/pages/HistoryPage.tsx,
    frontend/src/components/history/HistoryTable.tsx,
    frontend/src/components/history/HistoryFilters.tsx
  </files>
  <read_first>
    frontend/src/lib/types.ts,
    frontend/src/hooks/useExperiments.ts,
    frontend/src/hooks/usePredictionHistory.ts,
    frontend/src/components/shared/ConfidenceBadge.tsx,
    frontend/src/components/shared/ErrorState.tsx,
    frontend/src/components/shared/ResultIndicator.tsx,
    .planning/phases/05-dashboard/05-UI-SPEC.md
  </read_first>
  <action>
**A. ExperimentDetail component** — Create `frontend/src/components/experiments/ExperimentDetail.tsx`:

Props: `experiment: ExperimentResponse`.

Renders the expanded row content per UI-SPEC:
- Full hypothesis text (unwrapped) at 14px.
- Params section: 2-column grid of key-value pairs from `experiment.params`. Keys in muted text, values in mono font (`font-mono text-xs`). Use `Object.entries(experiment.params)` mapped to a `grid grid-cols-2 gap-x-8 gap-y-1` container.
- Features section: comma-separated list of `experiment.features` at 12px label text (`text-xs text-muted-foreground`).
- SHAP Top 5 section: for each item in `experiment.shap_top5`, render a horizontal bar. Feature name on left (`text-xs w-40`), bar segment with width proportional to `importance / max(importance)` as percentage, value on right. Bar color: `bg-blue-500`. Use simple CSS: a div with `style={{ width: \`\${(item.importance / maxImportance) * 100}%\` }}` and `h-4 bg-blue-500 rounded`.

**B. ExperimentTable component** — Create `frontend/src/components/experiments/ExperimentTable.tsx`:

Props: `experiments: ExperimentResponse[]`.

Sortable table with columns per UI-SPEC:
- `#` (48px, `w-12`): experiment_id in mono font (`font-mono text-xs`).
- `Hypothesis` (flex): hypothesis text, truncated at 60 chars with `truncate max-w-[300px]`.
- `2023 Val Acc` (100px, `w-[100px]`): formatted as `(val_accuracy_2023 * 100).toFixed(1) + "%"`.
- `2022 Val` (80px, `w-20`): formatted as `(val_accuracy_2022 * 100).toFixed(1) + "%"`.
- `2021 Val` (80px, `w-20`): formatted as `(val_accuracy_2021 * 100).toFixed(1) + "%"`.
- `Log Loss` (80px, `w-20`): formatted as `log_loss.toFixed(4)`.
- `Status` (80px, `w-20`): Badge — if `keep === true`: green Badge with text "Kept" (`bg-green-500/20 text-green-400`). If `keep === false`: red Badge with text "Reverted" (`bg-red-500/20 text-red-400`).

Sorting:
- Use React `useState` for sort state: `sortField` (default `"experiment_id"`) and `sortDir` (default `"desc"`).
- Sortable columns: `experiment_id` and `val_accuracy_2023`. Clicking a sortable header toggles direction or changes field.
- Show `ChevronUp`/`ChevronDown` icon (from lucide-react) next to the active sort column header. Use `cursor-pointer` on sortable headers.
- Sort the experiments array with `Array.from(experiments).sort(...)` before rendering (no mutation).

Expandable rows:
- Each table row is wrapped in a `Collapsible` from shadcn.
- `CollapsibleTrigger` wraps the `TableRow`. Clicking the row toggles the expanded state.
- `CollapsibleContent` renders below the row as a full-width `TableRow` with a single `TableCell` spanning all columns (`colSpan={7}`), containing `<ExperimentDetail experiment={experiment} />`.
- Use React `useState<number | null>` to track which experiment_id is expanded (only one at a time). Clicking an already-expanded row collapses it.

Row hover: `hover:bg-zinc-800/50 cursor-pointer transition-colors`.

**C. ExperimentsPage** — Rewrite `frontend/src/pages/ExperimentsPage.tsx`:

- Call `useExperiments()` hook.
- Set `document.title` to `"Experiments - NFL Predictor"`.
- Loading state: table skeleton — 5 rows of `Skeleton className="h-8 w-full"` stacked.
- Error state: `<ErrorState heading="Connection Failed" body="Could not reach the prediction API. Make sure the server is running on localhost:8000." onRetry={() => refetch()} />`.
- Empty state (when `data` is empty array): `<ErrorState heading="No Experiments Found" body="Run the training pipeline to generate experiment data." />` (no retry button).
- Normal state: `<h1 className="text-xl font-semibold mb-8">Experiment Scoreboard</h1>`, then `<ExperimentTable experiments={data} />`.

**D. HistoryFilters component** — Create `frontend/src/components/history/HistoryFilters.tsx`:

Props: `season: number | undefined`, `team: string | undefined`, `onSeasonChange: (season: number | undefined) => void`, `onTeamChange: (team: string | undefined) => void`, `availableSeasons: number[]`, `availableTeams: string[]`.

Render 2 Select dropdowns in a flex row (`flex gap-4 mb-6`):

Season Select:
- Use shadcn Select component.
- `SelectTrigger` with `className="w-[140px]"`.
- `SelectValue` placeholder "All Seasons".
- `SelectContent` with SelectItem for each season in `availableSeasons` (sorted descending). Plus an "All Seasons" option that sets season to undefined.

Team Select:
- `SelectTrigger` with `className="w-[160px]"`.
- `SelectValue` placeholder "All Teams".
- `SelectContent` with "All Teams" option (sets team to undefined) + SelectItem for each team in `availableTeams` (sorted alphabetically).

**E. HistoryTable component** — Create `frontend/src/components/history/HistoryTable.tsx`:

Props: `predictions: PredictionResponse[]`.

Render shadcn Table with columns per UI-SPEC:
- `Week` (60px, `w-[60px]`): week number.
- `Date` (100px, `w-[100px]`): `game_date` formatted as short date. If `game_date` is not null, parse and format as `new Date(game_date).toLocaleDateString("en-US", { month: "short", day: "numeric" })` (e.g. "Sep 7"). If null, show dash.
- `Matchup` (flex): `"{away_team} @ {home_team}"` at 14px.
- `Pick` (100px, `w-[100px]`): `predicted_winner` in semibold.
- `Confidence` (100px, `w-[100px]`): `{(confidence * 100).toFixed(1)}%` + `<ConfidenceBadge tier={confidence_tier} />` side by side.
- `Result` (80px, `w-20`): `<ResultIndicator correct={correct} />`.
- `Actual` (100px, `w-[100px]`): `actual_winner` or "Pending" in `text-muted-foreground text-sm`.

Row hover: `hover:bg-zinc-800/50`. Table scrolls horizontally on mobile via `overflow-x-auto` wrapper.

**F. HistoryPage** — Rewrite `frontend/src/pages/HistoryPage.tsx`:

- Import `useSearchParams` from `react-router`.
- Read `season` and `team` from URL search params: `searchParams.get("season")` parsed to number, `searchParams.get("team")`.
- Call `usePredictionHistory(season, team)` hook — queryKey includes filter params so TanStack Query refetches on filter change.
- Set `document.title` to `"History - NFL Predictor"`.
- Filter change handlers: create `setFilters(newSeason?: number, newTeam?: string)` that calls `setSearchParams(params)` with the new values. This keeps filter state in the URL for shareability per CONTEXT.md requirement.
- Compute `availableSeasons` from `data.predictions`: `[...new Set(data.predictions.map(p => p.season))].sort((a, b) => b - a)`. Compute `availableTeams`: `[...new Set(data.predictions.flatMap(p => [p.home_team, p.away_team]))].sort()`.
- Loading state: filter bar skeleton (2 Skeleton `h-10 w-36`) + table skeleton (8 rows of `Skeleton className="h-8 w-full"`).
- Error state: `<ErrorState heading="Connection Failed" body="Could not reach the prediction API. Make sure the server is running on localhost:8000." onRetry={() => refetch()} />`.
- Empty state (0 predictions): `<ErrorState heading="No Predictions Yet" body="Predictions will appear here after the model processes game data." />`.
- Normal state:
  - `<h1 className="text-xl font-semibold mb-1">Prediction History</h1>`
  - Summary stat bar: `<div className="flex gap-4 mb-6 text-sm text-muted-foreground">` showing `{summary.correct}/{summary.total} correct` and `({(summary.accuracy * 100).toFixed(1)}%)` if accuracy is not null.
  - `<HistoryFilters season={season} team={team} onSeasonChange={...} onTeamChange={...} availableSeasons={...} availableTeams={...} />`
  - `<HistoryTable predictions={data.predictions} />`
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/experiments/ExperimentTable.tsx contains "Collapsible"
    - frontend/src/components/experiments/ExperimentTable.tsx contains "ChevronUp" or "ChevronDown"
    - frontend/src/components/experiments/ExperimentTable.tsx contains "Kept"
    - frontend/src/components/experiments/ExperimentTable.tsx contains "Reverted"
    - frontend/src/components/experiments/ExperimentTable.tsx contains "val_accuracy_2023"
    - frontend/src/components/experiments/ExperimentTable.tsx contains "experiment_id"
    - frontend/src/components/experiments/ExperimentTable.tsx contains "font-mono"
    - frontend/src/components/experiments/ExperimentDetail.tsx contains "shap_top5"
    - frontend/src/components/experiments/ExperimentDetail.tsx contains "params"
    - frontend/src/components/experiments/ExperimentDetail.tsx contains "features"
    - frontend/src/pages/ExperimentsPage.tsx contains "useExperiments"
    - frontend/src/pages/ExperimentsPage.tsx contains "ExperimentTable"
    - frontend/src/pages/ExperimentsPage.tsx contains "Experiment Scoreboard"
    - frontend/src/components/history/HistoryTable.tsx contains "ResultIndicator"
    - frontend/src/components/history/HistoryTable.tsx contains "ConfidenceBadge"
    - frontend/src/components/history/HistoryTable.tsx contains "predicted_winner"
    - frontend/src/components/history/HistoryTable.tsx contains "actual_winner"
    - frontend/src/components/history/HistoryFilters.tsx contains "Select"
    - frontend/src/components/history/HistoryFilters.tsx contains "All Teams"
    - frontend/src/components/history/HistoryFilters.tsx contains "All Seasons"
    - frontend/src/pages/HistoryPage.tsx contains "useSearchParams"
    - frontend/src/pages/HistoryPage.tsx contains "usePredictionHistory"
    - frontend/src/pages/HistoryPage.tsx contains "HistoryFilters"
    - frontend/src/pages/HistoryPage.tsx contains "HistoryTable"
    - frontend/src/pages/HistoryPage.tsx contains "Prediction History"
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Experiments page renders a sortable table with all experiment data, expandable rows showing params/features/SHAP bars/hypothesis, and Kept/Reverted status badges. History page renders all past predictions with correct/incorrect icons, season/team Select filters synced to URL params, and summary stats bar. Both pages handle loading, error, and empty states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all 4 dashboard pages render correctly</name>
  <files>None (verification only)</files>
  <action>
Human visual verification of the complete 4-page dashboard against the UI-SPEC and DASH requirements.

1. Start the FastAPI server: `cd E:/ClaudeCodeProjects/GamePredictor && python -m uvicorn api.main:app --reload`
2. Start the frontend dev server: `cd E:/ClaudeCodeProjects/GamePredictor/frontend && npm run dev`
3. Open `http://localhost:5173` in browser

**This Week's Picks (`/`):**
- Verify pick cards render with team names, predicted winner highlighted, win probability percentage, and confidence tier badge (High/Medium/Low)
- Verify cards have colored left border matching confidence tier
- Verify responsive grid: 3 columns on desktop, 2 on tablet, 1 on mobile

**Season Accuracy (`/accuracy`):**
- Verify 3 summary cards: Model record, vs Always-Home, vs Better-Record
- Verify "Beating +X%" or "Behind X%" badges in green/red
- Verify week-by-week breakdown table below summary cards

**Experiment Scoreboard (`/experiments`):**
- Verify table shows all experiments with ID, hypothesis, accuracies, log loss, Kept/Reverted badges
- Verify clicking a column header sorts the table
- Verify clicking a row expands to show params, features, SHAP top 5 bars, full hypothesis

**Prediction History (`/history`):**
- Verify table shows predictions with CircleCheck (green) / CircleX (red) / Pending indicators
- Verify season and team dropdown filters work
- Verify changing filters updates the URL query params (shareable URLs)
- Verify summary stats bar shows correct/total

**Sidebar (all pages):**
- Verify navigation works between all 4 pages
- Verify active route is highlighted in blue
- Verify model status bar at bottom shows experiment ID and val accuracy
  </action>
  <verify>Human approval: user types "approved" or describes issues</verify>
  <done>All 4 pages visually verified against UI-SPEC. DASH-01, DASH-02, DASH-03, and DASH-04 requirements confirmed satisfied.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` exits 0 (TypeScript compilation + Vite build succeeds)
2. All 4 page components exist and import their respective hooks
3. Experiment table uses Collapsible for expandable rows
4. History page uses useSearchParams for URL-synced filters
5. All pages handle loading (Skeleton), error (ErrorState with retry), and empty states
6. Visual verification confirms all 4 DASH requirements are satisfied
</verification>

<success_criteria>
- DASH-01: This Week's Picks page shows games with predicted winner, win probability, confidence tier
- DASH-02: Season Accuracy page shows model record vs both baselines with Beating/Behind indicators
- DASH-03: Experiment Scoreboard shows sortable table with all experiment data and expandable detail rows
- DASH-04: Prediction History shows filterable table with correct/incorrect indicators and URL-synced filters
- All pages render loading skeletons during fetch
- All pages show error state with retry button on API failure
- `npm run build` succeeds with no TypeScript errors
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-02-SUMMARY.md`
</output>

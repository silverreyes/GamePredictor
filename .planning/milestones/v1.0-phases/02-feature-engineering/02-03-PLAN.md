---
phase: 02-feature-engineering
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [sql/init.sql]
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04, FEAT-05]
gap_closure: true

must_haves:
  truths:
    - "sql/init.sql game_features DDL column names match pipeline output columns exactly"
    - "store_game_features() can insert pipeline output into game_features table without SQL column errors"
  artifacts:
    - path: "sql/init.sql"
      provides: "Corrected game_features DDL with all 14 rolling columns matching pipeline output"
      contains: "home_rolling_off_epa_per_play"
  key_links:
    - from: "features/build.py (store_game_features)"
      to: "sql/init.sql (game_features DDL)"
      via: "SQLAlchemy table reflection at runtime"
      pattern: "home_rolling_off_epa_per_play.*REAL"
---

<objective>
Fix sql/init.sql game_features DDL to match pipeline output column names

Purpose: The verification report found that 6 DDL column names are wrong and 4 columns are missing relative to what features/build.py actually outputs. store_game_features() would fail at runtime with SQL column-not-found errors. This plan corrects the DDL so the persistence layer aligns with the in-memory pipeline.

Output: Updated sql/init.sql with correct game_features table DDL
</objective>

<execution_context>
@C:/Users/silve/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/silve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feature-engineering/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix game_features DDL column names and add missing columns</name>
  <files>sql/init.sql</files>
  <read_first>
    - sql/init.sql (current DDL with wrong column names)
    - features/build.py (source of truth for actual column names -- read lines 120-137 for rolling column names, lines 156-168 for home_/away_ prefix logic)
  </read_first>
  <action>
Edit the game_features CREATE TABLE block in sql/init.sql (lines 89-120). Make these exact changes:

RENAME 6 columns:
1. home_rolling_off_epa -> home_rolling_off_epa_per_play
2. home_rolling_def_epa -> home_rolling_def_epa_per_play
3. home_rolling_win_rate -> home_rolling_win
4. away_rolling_off_epa -> away_rolling_off_epa_per_play
5. away_rolling_def_epa -> away_rolling_def_epa_per_play
6. away_rolling_win_rate -> away_rolling_win

ADD 4 missing columns (after home_rolling_turnover_diff and away_rolling_turnover_diff respectively):
7. home_rolling_turnovers_committed REAL
8. home_rolling_turnovers_forced REAL
9. away_rolling_turnovers_committed REAL
10. away_rolling_turnovers_forced REAL

The final rolling columns section should be (in this exact order):

    -- Home team rolling features
    home_rolling_off_epa_per_play REAL,
    home_rolling_def_epa_per_play REAL,
    home_rolling_point_diff REAL,
    home_rolling_turnovers_committed REAL,
    home_rolling_turnovers_forced REAL,
    home_rolling_turnover_diff REAL,
    home_rolling_win REAL,

    -- Away team rolling features
    away_rolling_off_epa_per_play REAL,
    away_rolling_def_epa_per_play REAL,
    away_rolling_point_diff REAL,
    away_rolling_turnovers_committed REAL,
    away_rolling_turnovers_forced REAL,
    away_rolling_turnover_diff REAL,
    away_rolling_win REAL

Do NOT modify any other table DDL (raw_pbp, schedules, ingestion_log).
Do NOT modify features/build.py or features/definitions.py (CLAUDE.md locks those files).
  </action>
  <verify>
    <automated>python -c "
import re
with open('sql/init.sql') as f:
    ddl = f.read()
# Check renamed columns exist
assert 'home_rolling_off_epa_per_play REAL' in ddl, 'Missing home_rolling_off_epa_per_play'
assert 'home_rolling_def_epa_per_play REAL' in ddl, 'Missing home_rolling_def_epa_per_play'
assert 'home_rolling_win REAL' in ddl, 'Missing home_rolling_win'
assert 'away_rolling_off_epa_per_play REAL' in ddl, 'Missing away_rolling_off_epa_per_play'
assert 'away_rolling_def_epa_per_play REAL' in ddl, 'Missing away_rolling_def_epa_per_play'
assert 'away_rolling_win REAL' in ddl, 'Missing away_rolling_win'
# Check old column names are gone
assert 'home_rolling_off_epa REAL' not in ddl, 'Old home_rolling_off_epa still present'
assert 'home_rolling_def_epa REAL' not in ddl, 'Old home_rolling_def_epa still present'
assert 'home_rolling_win_rate' not in ddl, 'Old home_rolling_win_rate still present'
assert 'away_rolling_off_epa REAL' not in ddl, 'Old away_rolling_off_epa still present'
assert 'away_rolling_def_epa REAL' not in ddl, 'Old away_rolling_def_epa still present'
assert 'away_rolling_win_rate' not in ddl, 'Old away_rolling_win_rate still present'
# Check added columns exist
assert 'home_rolling_turnovers_committed REAL' in ddl, 'Missing home_rolling_turnovers_committed'
assert 'home_rolling_turnovers_forced REAL' in ddl, 'Missing home_rolling_turnovers_forced'
assert 'away_rolling_turnovers_committed REAL' in ddl, 'Missing away_rolling_turnovers_committed'
assert 'away_rolling_turnovers_forced REAL' in ddl, 'Missing away_rolling_turnovers_forced'
# Count total rolling columns (should be 14: 7 home + 7 away)
rolling_count = len(re.findall(r'(?:home|away)_rolling_\w+ REAL', ddl))
assert rolling_count == 14, f'Expected 14 rolling columns, found {rolling_count}'
print('All 14 rolling columns verified. DDL matches pipeline output.')
"</automated>
  </verify>
  <acceptance_criteria>
    - sql/init.sql contains "home_rolling_off_epa_per_play REAL" (not "home_rolling_off_epa REAL")
    - sql/init.sql contains "home_rolling_def_epa_per_play REAL" (not "home_rolling_def_epa REAL")
    - sql/init.sql contains "home_rolling_win REAL" (not "home_rolling_win_rate")
    - sql/init.sql contains "away_rolling_off_epa_per_play REAL" (not "away_rolling_off_epa REAL")
    - sql/init.sql contains "away_rolling_def_epa_per_play REAL" (not "away_rolling_def_epa REAL")
    - sql/init.sql contains "away_rolling_win REAL" (not "away_rolling_win_rate")
    - sql/init.sql contains "home_rolling_turnovers_committed REAL"
    - sql/init.sql contains "home_rolling_turnovers_forced REAL"
    - sql/init.sql contains "away_rolling_turnovers_committed REAL"
    - sql/init.sql contains "away_rolling_turnovers_forced REAL"
    - Total rolling columns in game_features DDL = 14 (7 home + 7 away)
    - raw_pbp, schedules, and ingestion_log table DDLs are unchanged
    - All existing tests still pass: pytest features/tests/ -x -q exits 0
  </acceptance_criteria>
  <done>game_features DDL in sql/init.sql has exactly 14 rolling columns (7 home + 7 away) whose names match the pipeline output from features/build.py, with no old abbreviated names remaining</done>
</task>

</tasks>

<verification>
1. Verify renamed columns: grep for "home_rolling_off_epa_per_play" in sql/init.sql (must exist)
2. Verify old names gone: grep for "home_rolling_off_epa REAL" in sql/init.sql (must NOT exist)
3. Verify added columns: grep for "home_rolling_turnovers_committed" in sql/init.sql (must exist)
4. Verify column count: 14 rolling columns total in game_features DDL
5. Verify existing tests: pytest features/tests/ -x -q passes (no regressions)
</verification>

<success_criteria>
- sql/init.sql game_features DDL declares all 14 rolling columns matching pipeline output
- No old abbreviated column names remain in the DDL
- All existing feature tests (24 tests) continue to pass
- Verification truth #12 gap is closed: DDL column names now match pipeline output column names for store_game_features()
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-engineering/02-03-SUMMARY.md`
</output>
